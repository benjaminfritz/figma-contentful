{
  "name": "figma-contentful-sync",
  "private": true,
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "watch": "node scripts/build.mjs --watch",
    "preview": "node scripts/preview-server.mjs"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.117.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.0"
  }
}
